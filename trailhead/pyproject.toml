[project]
name = "trailhead"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "trailhead-edu==0.4.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
